# 검증 기록

- 기준 저장소: https://github.com/khj320200/competition-rate-dashboard
- 기준 커밋: `a8318b6` (main)
- 검증일: 2026-09-08, Asia/Seoul
- 로컬 런타임: Node.js v24.13.0
- 검증 범위: 수정본 로컬 실행 및 기존 Render 배포의 읽기 진단. GitHub push 및 Render 배포는 수행하지 않음.

## 자동 검증

`npm run check` 문법 검사 통과. `npm test` 15개 테스트 통과.

- 진학어플라이 세션/상세의 프록시 적용과 유웨이 직접 연결 유지
- 프록시 인증, 잘못된 설정, timeout, 403/429/407 분류
- 실제 로컬 CONNECT 프록시의 인증 헤더와 인증 실패 처리
- 서버 설정·소스 파일의 정적 URL 차단
- 실시간 파싱 → 원문 실패 → 이전 캐시의 stale 반환
- HTTP 200 차단 페이지와 정상 검색 결과 없음 구분
- 잘못된 원문 URL과 리다이렉트 차단
- 화면의 캐시 경고, 기존 수치/시각 보존, 정상 새로고침

## 실제 원문 조회

수정본의 `npm run diagnose`를 작업 PC에서 실행해 모두 성공했습니다.

| 대상 | 결과 |
| --- | --- |
| 상태 API | HTTP 200, 직접 연결 |
| 대학명 검색: 가톨릭 | HTTP 200, 9개 검색 결과 |
| 진학어플라이: 가톨릭대학교 | HTTP 200, 273개 행, 원문 2026-09-08 오후 11:00 |
| 유웨이: 극동대학교 | HTTP 200, 58개 행, 원문 2026-09-08 23:30 |

임시 인증 CONNECT 프록시를 작업 PC의 loopback 주소에만 열어 실제 진학어플라이 상세도 읽었습니다. 상세 HTTP 200 및 `tableRatio3` 존재를 확인했습니다. TLS 인증서 검증은 켠 상태였습니다. 같은 호스트 루트(`/`)는 HTTP 403이었으므로 루트의 상태만으로 상세 크롤링의 실패를 판단해서는 안 됩니다. 임시 프록시는 검증 직후 종료했습니다.

실제 상용 국내 프록시는 아직 구성하지 않았습니다. Render에 수정본과 접속 경로를 반영한 뒤 다시 실측해야 최종 해결을 확인할 수 있습니다.
